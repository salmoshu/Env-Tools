# 统一行尾：仓库内一律 LF，消除 Windows/WSL 跨侧 CRLF 噪音
* text=auto

# Shell 脚本任何平台都保持 LF（CRLF 会导致 bash 解析失败）
*.sh   text eol=lf
*.bash text eol=lf

# Windows 批处理与 PowerShell 脚本保持 CRLF
*.cmd  text eol=crlf
*.bat  text eol=crlf
*.ps1  text eol=crlf

# 图标与图片按二进制提交，不做行尾转换
*.png  -text
*.ico  -text
